# Relative, not an absolute host: Caddy (see root docker-compose.yml +
# Caddyfile) reverse-proxies same-origin /api/* to the backend container,
# stripping the /api prefix - so this built frontend image works on any
# domain without a rebuild.
VITE_API_BASE_URL=/api
VITE_USE_SAMPLE_ASSETS=false
